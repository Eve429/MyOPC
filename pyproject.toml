[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
# 00_PAST 只读归档永不扫描。规则集用默认；排版统一 ruff format
# （line-length 120，格式器默认参数即项目风格，2026-08-22 起）。
extend-exclude = ["00_PAST"]
line-length = 120
