# 当前项目架构复核计划

## 目标

基于 2026-08-22 当前工作树重新理解 MyOPC，不沿用旧会话架构假设；本次仅只读审查，不修改生产代码、配置、测试或用户数据。

## 阶段

- [x] 记录 Git 基线、工作树状态与项目规则。
- [x] 核对系统架构、数据模型、契约和主入口。
- [x] 沿 Macro–Core、MB-OPC、ILT 三条链路核对源码接口和状态边界。
- [x] 核对测试布局并运行适当门禁，确认当前可验证基线。
- [x] 输出当前架构摘要、已确认事实和仍需暴露的不确定性。

## 约束

- 不修改 `layout/`、`geometry/`、`00_PAST/`。
- 不修改当前用户工作树文件：两个 TOML、一个已跟踪 GDS、一个未跟踪 GDS。
- 不把历史报告或旧计划直接当作当前源码事实。

## 错误记录

- Windows 默认 `D:\app\miniforge\python.exe` 未安装 pytest，首次收集失败；改用项目记录中已验证的 WSL `~/miniconda3/envs/myopc312/bin/python`。
- 首次追加错误记录时补丁假定了不存在的表头而失败；读取实际文件后改为匹配现有“暂无”文本。
- 完整 `pytest -q` 首次执行在工具 244 秒上限被终止，终止前没有测试失败输出；这不是测试失败。改用更长的显式超时重新执行并要求首个失败立即退出。

## 结论

- 当前架构已按两类优化变量明确分为 edge problem 与 pixel problem，并共享 grid、lithography、macro 生命周期和最终合并边界。
- 当前 2 种 MB-OPC、3 种 ILT 均有生产入口和测试；Multilevel 仅有 draft 规格，不是当前能力。
- 当前完整门禁：695 passed、1 skipped、3 个预期 UserWarning；静态和格式门禁通过。
